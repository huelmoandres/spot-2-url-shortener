[build]
dockerfilePath = "Dockerfile"
watchPatterns = ["backend/**", ".github/workflows/backend-ci.yml"]

[deploy]
healthcheckPath = "/api/urls"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
